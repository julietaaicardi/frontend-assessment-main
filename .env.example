# API Configuration
# Development: Use proxy to avoid CORS issues
# Production: Use direct API calls (assuming proper CORS setup)
NUXT_PUBLIC_API_BASE_URL=https://directus.altura.io
NUXT_PUBLIC_API_TIMEOUT=10000

# Environment
NODE_ENV=development
